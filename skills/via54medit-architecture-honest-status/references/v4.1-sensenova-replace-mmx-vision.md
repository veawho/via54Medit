# v4.1 sensenova 替代 mmx vision 实战

> 配套: `via54medit-architecture-honest-status` §4.8

## 1. 触发场景

mmx vision 报 `API error: 已达到 Token Plan 用量上限` → 立即改 sensenova.

## 2. sensenova vs mmx 对比

| 对比项 | mmx vision | **sensenova-6.7-flash-lite** |
|--------|-----------|--------------------------|
| 费用 | Token Plan, 经常超限 | **免费** (pricing=0) |
| Context | 有限 | **262K tokens** |
| 敏感词 | "Hong Kong" 触发审查 | 无审查 (国内 API) |
| 安装 | npm mmx-cli | 0 安装, 纯 API |
| 速度 | 慢 | 快 |
| 视觉复核能力 | 强 | 强 |

## 3. API 详情

```python
base_url: https://token.sensenova.cn/v1
model: sensenova-6.7-flash-lite
input_modalities: ["text", "image"]
output: image base64 in content array
pricing: 0 (免费)
```

## 4. 实现位置

```
via54Medit/scripts/
├── sensenova_vision.py    (核心 API 调用 + base64 + --json / --save)
└── l3_vision_verify.py    (L3 流程包装器)
```

## 5. CLI 用法

```bash
# 单次复核
python3.11 scripts/sensenova_vision.py <image.jpg> "PPT引用语义"

# 结构化 JSON 输出 (但偏差比 non-JSON 大, 默认用 non-JSON)
python3.11 scripts/sensenova_vision.py <image.jpg> "prompt" --json

# L3 集成包装器
python3.11 scripts/l3_vision_verify.py <image.jpg> "allegation_text"
```

## 6. 4 Pn-x 实测 (全成功)

| Pn-x | 关键识别 | 命中数 |
|------|---------|:----:|
| P3-1 (Kudo HBSN 2022) | HIMALAYA + ORR 20.1% + CR 3.1% | 3 |
| P29-1 (Song YG 2024) | 8.42% / 4.42% / 2.06% / 2.11x | 4 |
| P33-11 (Song YG 2024) | Grade III/IV bleeding 4.42% | 1 |
| P30-1 (AHELP NCT) | 47.9% 高血压 + 21% 蛋白尿 (any-grade) | 2 |

## 7. 经验沉淀

1. **不要预设 Token Plan 还能用** — 立即改 sensenova
2. **sensenova 对 1 页 PDF 的 highlight 图** < 30s 响应
3. **--json 模式** 比 non-JJSON 模式偏差大, 默认用 non-JSON
4. **图像分辨率不需要太高** — 150 DPI 已够用
5. **香港/台湾敏感词绕过**: 用宽泛临床描述, 不直接提地名

## 8. sensenova_vision.py 关键代码

```python
import base64, json, urllib.request, os

def call_sensenova(image_path, prompt):
    with open(image_path, 'rb') as f:
        b64 = base64.b64encode(f.read()).decode()
    payload = {
        'model': 'sensenova-6.7-flash-lite',
        'messages': [{
            'role': 'user',
            'content': [
                {'type': 'text', 'text': prompt},
                {'type': 'image_url', 'image_url': {'url': f'data:image/jpeg;base64,{b64}'}}
            ]
        }]
    }
    req = urllib.request.Request(
        'https://token.sensenova.cn/v1/chat/completions',
        data=json.dumps(payload).encode(),
        headers={
            'Authorization': 'Bearer ' + os.environ['SENSENOVA_API_KEY'],
            'Content-Type': 'application/json',
        }
    )
    with urllib.request.urlopen(req, timeout=60) as resp:
        return json.loads(resp.read())['choices'][0]['message']['content']
```

## 9. 触发关键词

- sensenova / sensenova-6.7-flash-lite / 替 mmx vision
- Token Plan 上限 / 视觉复核失败
- L3 视觉复核
