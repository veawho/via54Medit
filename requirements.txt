# via54Medit Python 工具链依赖 (跨平台部署, 2026-08-21)
# 安装: pip install -r requirements.txt
# 部署/更新后请先跑深度扫描(按平台只装相关且缺失的, 含 OCR 与 mmx-cli 视觉能力):
#   python scripts/deploy_scan.py            # 扫描 + 补齐缺口
#   python scripts/deploy_scan.py --check     # 只扫描
# 或一键: python scripts/bootstrap_device.py
# 注意: mmx-cli **不在本文件里** —— 它是 npm 包(npm install -g mmx-cli), PyPI 上没有这个发行版。
#
# 平台说明:
#   pywin32 仅 Windows 需要 (PowerPoint/Word 的 COM 渲染), 其他平台自动跳过。
#   PPT / Word 的**版式**只由微软引擎产出: 桌面版 PowerPoint / Word, 或**显式**指定
#   RENDER_ENGINE=graph 走 Microsoft Graph 的在线转换。不会退回会重新排版的第三方引擎 ——
#   判定标准与证据见 docs/ppt-render-fidelity.md。因此 Python 侧**不需要**装 LibreOffice。
#   pytest 用于运行 scripts/ 下测试 (test_tma_pipeline.py, hl_v3_final/test_hl_lib.py)。
#
# 系统二进制 (非 pip, 非可选):
#   - Microsoft PowerPoint / Word : PPT / Word 的版式渲染 (桌面版; 没有就渲染不出来)
#   - poppler-utils (pdftoppm)    : 可选, 仅当 RENDER_RASTERIZER=pdftoppm; 默认用 pip 的 pymupdf
#   - Chrome / Edge / Chromium    : CDP 调试实例 (`medit browser start` 自动探测启动)
#
# 部署后先自检渲染是否真的能出图: python3 scripts/render_doctor.py

pymupdf>=1.24
python-pptx>=0.6.23
Pillow>=10.0
pywin32>=306; sys_platform == "win32"

# 测试
pytest>=8.0
