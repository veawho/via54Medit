# 8 列标准 + 同文献合并规则(2026-08-14 定稿, 注入 via54Medit)

> 用户硬要求: **本地表与在线表在逻辑、列、规则上与雷管方案完全一致**。
> 本文件是 via54Medit 仓库内的权威摘要; 完整规范与脚本见:
> - `docs/HIGHLIGHT机制与算法规范_v3_FINAL.md`(highlight 机制全文)
> - `scripts/hl_v3_final/`(hl_lib.py + pipeline 脚本 + 105 句子脚本示例)
> - `~/.hermes/skills/via54medit-literature-pipeline/references/`(skill 侧同源副本)

## 一、两张表(8 列 A-H, 同构)

| 表 | 位置 | 列 |
|---|---|---|
| 本地表 | `_citation_table/tma_citation_table.csv` | PN \| 幻灯片 \| 引用序号 \| 引用 \| PDF大小 \| 已Highlight \| MD5 \| 页数 |
| 在线表(飞书) | 飞书 sheet `Nf84sqBbqh0zcjtUCAZcGRkknMe` | PPT页 \| 第几条 \| 引用语义（上下文）\| PPT中的文献引用 完整字段 \| DOI \| 类型 \| 对应PDF文件 \| 来源链接 → 阅读全文 |

两表同构: 106 行(每 Pn-x 一行), 数据全部从雷管方案目录实测派生(MD5/页数/PDF大小/高亮图片数),
不手工填。生成: `scripts/hl_v3_final/align_tables.py`(本地+在线同构 CSV) → `leiguan_table.py --write`(写飞书)。

## 二、H 列卡片格式

```
🎯 Row {N} ({Pn-x}) — {文献标题}

【📄 主文件 (按 PPT 引用内容部署)】
  - 本地路径: `{Pn-x}/{Pn-x}_main.pdf`     ← 反引号纯文本(file:// 中文路径飞书会截断!)
  - 大小: {size} KB, {pages} 页
  - MD5: `{md5前8}` (完整 `{md5}`)
  - 📥 在线访问: {DOI 解析 / PMC / UpToDate / 暂无 (中文期刊, 见本地 PDF)}

【🔗 DOI 状态】{[doi](https://doi.org/{doi}) | 备注: 无 DOI (中文期刊 / UpToDate / 未解析)}
【📍 PPT 真实内容位置】标号 + 位置原文
【🔎 Highlight 情况】{N} 处高亮, {N} 张图, 句子首句
【📌 文献类型】指南 / 病例报告 / RCT 试验 / 数据 / 文献
【🗂 完整入口】本地 + 在线访问 + DOI + PPT + Highlight 图(4-tier 链接)
```

要点: 4-tier 链接 = DOI / 在线访问 / PPT 位置 / Highlight 图; 含 URL 单元格飞书自动转 url 段;
不想链接化的 URL 加 `\u200b` 前缀; file:// 中文路径用反引号纯文本。

## 三、同文献目录合并规则

- 合并判定 = **引用文本指向同一文献**(不能只看 MD5: 同一文献不同下载版本 MD5 不同仍要合并)
- 新目录名 = 成员 Pn-x **按数字顺序下划线连接**: `P3-1 + P4-1 → P3-1_P4-1`
- ⚠️ 旧格式 `Pn1-x1Pn2-x2`(无下划线)已废弃
- TMA 实测: 28 个 Pn-x → 12 组合并, 106 → 90 目录(清单: `~/.hermes/skills/via54medit-literature-pipeline/references/merge-rules.md`)
- 合并前必须确认引用一致或同 DOI; 合并保留各成员全部文件(highlight.pdf/main.pdf/verify.json)

## 四、交付基线(雷管方案)

```
step4_highlight_106目录_合并DOI/{Pn-x}/
├── {Pn-x}_highlight.pdf       # 高亮 PDF(先清旧 annots 再生成)
├── {Pn-x}_highlight_pNNN.png  # 根目录: 仅"有高亮"的页面图
├── {Pn-x}_highlight_pages/    # 全部页面图(存档, 含无高亮页)
├── {Pn-x}_main.pdf            # 源文献(与 slide 引用匹配, md5 记录)
└── {Pn-x}_verify.json         # md5/pages/highlights 句子/annot_count/download_status
```

## 五、URL 可达性验证

- 用 DOI 列直接验证(正则提取 URL 会被 DOI 内括号截断)
- 判定: 200 可达 / 403-Cloudflare 受限(标注"受限", 不删) / 连接失败才算失效
- TMA 实测: 142 URL, 94 可达 + 48 受限 + 0 失效
